<%= form_with(model: matrics_detail, local: true) do |form| %>
  <% if matrics_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(matrics_detail.errors.count, "error") %> prohibited this matrics_detail from being saved:</h2>

      <ul>
      <% matrics_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year %>
  </div>

  <div class="field">
    <%= form.label :wrote %>
    <%= form.number_field :wrote %>
  </div>

  <div class="field">
    <%= form.label :passed %>
    <%= form.number_field :passed %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
